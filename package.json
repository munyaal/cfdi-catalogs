{
  "name": "@munyaal/cfdi-catalogs",
  "version": "1.0.0-beta.2",
  "files": [
    "dist/**"
  ],
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Librería que incluye catálogos SAT para CFDI 4.0",
  "author": "Munyaal Apps",
  "license": "MIT",
  "scripts": {
    "build": "rimraf ./dist && tsc --build",
    "pretest": "npm run build",
    "test": "node ./dist/test/index.js"
  },
  "devDependencies": {
    "rimraf": "^4.1.2",
    "typescript": "^4.9.4"
  }
}
